from setuptools import setup, find_packages

setup(
    name="cptdl",
    version="0.1.0",
    packages=find_packages(),  # automatically find submodules
    install_requires=[],       # list dependencies here, e.g. ['numpy', 'pandas']
    description="cptdl module from PyCPT",
    author="IRI PyCPT",
)
